[build]
builder = "NIXPACKS"

[build.nixpacksPlan]
phases = { setup = ["python311"], install = ["pip install -r requirements.txt"] }

[deploy]
startCommand = "uvicorn server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
